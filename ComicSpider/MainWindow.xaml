<Window
		x:Class="ComicSpider.MainWindow"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:tb="http://www.hardcodet.net/taskbar"
		Title="Comic Spider" Height="120" Width="240" MinHeight="120" MinWidth="240"
		Closed="Window_Closed"
		Topmost="True"
		Icon="/Comic%20Spider;component/Images/y.s.ico" DragEnter="Window_DragEnter" Drop="Window_Drop" AllowDrop="True" WindowStartupLocation="CenterScreen">
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="5*" />
			<ColumnDefinition Width="3*" />
		</Grid.ColumnDefinitions>
		<Grid>
			<Grid.RowDefinitions>
				<RowDefinition Height="*" />
				<RowDefinition Height="32" />
			</Grid.RowDefinitions>
			<TextBlock TextWrapping="Wrap" FontFamily="Verdana" VerticalAlignment="Center" Foreground="#FF6C6C6C" HorizontalAlignment="Center">Drag and drop link here.</TextBlock>
			<CheckBox x:Name="cb_latest_volume_only" IsChecked="True" Grid.Row="1" VerticalAlignment="Center" Margin="5">latest volume only</CheckBox>
		</Grid>
		<Grid Grid.Column="1">
			<Grid.RowDefinitions>
				<RowDefinition Height="*" />
				<RowDefinition Height="*" />
			</Grid.RowDefinitions>
			<Button x:Name="btn_hide" x:FieldModifier="private" Content="Hide" Click="btn_hide_Click" Margin="5" Background="White" />
			<Button x:Name="btn_dashboard" x:FieldModifier="private" Grid.Row="1" Content="Dashboard" Click="btn_dashboard_Click" Margin="5" Background="White" />
		</Grid>
		<tb:TaskbarIcon x:Name="tray" IconSource="/Comic%20Spider;component/Images/ys_16.ico" TrayLeftMouseDown="tray_TrayLeftMouseDown" TrayToolTipOpen="tray_TrayToolTipOpen" Visibility="Collapsed">
			<tb:TaskbarIcon.TrayToolTip>
				<Border Padding="10,5" CornerRadius="3" Panel.ZIndex="100" Opacity="0.9">
					<Border.Background>
						<ImageBrush ImageSource="pack://application:,,,/Comic Spider;component/Images/main_bg.jpg" Stretch="UniformToFill" TileMode="None" />
					</Border.Background>
					<Border.Effect>
						<DropShadowEffect Color="#FF4B4B4B" Direction="270" ShadowDepth="2" BlurRadius="5" Opacity="0.7"/>
					</Border.Effect>
					<StackPanel>
						<StackPanel Orientation="Horizontal">
							<TextBlock Text="Comic Spider" Margin="5" Foreground="Black" FontSize="16" />
							<TextBlock Text="Download volume: " Foreground="Black" Margin="5" VerticalAlignment="Bottom" />
							<TextBlock x:Name="txt_main_progress" Text="10/90" Margin="0,5,5,5" Foreground="Black" FontWeight="Bold" VerticalAlignment="Bottom" />
						</StackPanel>
						<TextBlock Text="{Binding ToolTipText}" Foreground="#FF7C3958" Margin="5" />
					</StackPanel>
				</Border>
			</tb:TaskbarIcon.TrayToolTip>
		</tb:TaskbarIcon>
	</Grid>
</Window>
