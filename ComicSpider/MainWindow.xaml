<Window x:Class="ComicSpider.MainWindow"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:ComicSpider="clr-namespace:ComicSpider"
		Title="Comic Spider" Height="500" Width="650"
		xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
		Loaded="Window_Loaded"
		Closed="Window_Closed" Icon="/Comic%20Spider;component/Images/y.s.ico">
	<Window.Resources>
		<DataTemplate x:Key="HeaderTemplateArrowUp">
			<DockPanel>
				<TextBlock HorizontalAlignment="Center" Text="{Binding}"/>
				<Path x:Name="arrow"
		   StrokeThickness = "1"					  
		   Fill            = "gray"
		   Data            = "M 5,10 L 15,10 L 10,5 L 5,10"/>
			</DockPanel>
		</DataTemplate>
		<DataTemplate x:Key="HeaderTemplateArrowDown">
			<DockPanel>
				<TextBlock HorizontalAlignment="Center" Text="{Binding }"/>
				<Path x:Name="arrow"
			  StrokeThickness = "1"					  
			  Fill            = "gray"
			  Data            = "M 5,5 L 10,10 L 15,5 L 5,5"/>
			</DockPanel>
		</DataTemplate>
	</Window.Resources>
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*" />
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="72" />
			<RowDefinition Height="36" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>
		<Grid Grid.Row="2">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*" />
				<ColumnDefinition Width="*" />
			</Grid.ColumnDefinitions>
			<ListView Name="vol_list" Margin="5,5,0,5" SelectionChanged="vol_list_SelectionChanged" GridViewColumnHeader.Click="GridViewColumnHeaderClickedHandler">
				<ListView.View>
					<GridView>
						<GridViewColumn Header="State" Width="50" DisplayMemberBinding="{Binding State}"></GridViewColumn>
						<GridViewColumn Header="Name" Width="200" DisplayMemberBinding="{Binding Name}"></GridViewColumn>
					</GridView>
				</ListView.View>
			</ListView>
			<GridSplitter HorizontalAlignment="Left" Name="gridSplitter1" Width="5" Grid.Column="1" Margin="0,5" Background="White" />
			<ListView Name="Page_list" Grid.Column="1" Margin="5" GridViewColumnHeader.Click="GridViewColumnHeaderClickedHandler">
				<ListView.View>
					<GridView>
						<GridViewColumn Header="State" Width="50" DisplayMemberBinding="{Binding State}"></GridViewColumn>
						<GridViewColumn Header="Url" Width="200">
							<GridViewColumn.CellTemplate>
								<DataTemplate>
									<TextBlock Text="{Binding Url}" HorizontalAlignment="Right"></TextBlock>
								</DataTemplate>
							</GridViewColumn.CellTemplate>
						</GridViewColumn>
					</GridView>
				</ListView.View>
			</ListView>
		</Grid>
		<Grid Grid.ColumnSpan="3">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="70" />
				<ColumnDefinition Width="416*" />
				<ColumnDefinition Width="48" />
				<ColumnDefinition Width="64" />
			</Grid.ColumnDefinitions>
			<Grid.RowDefinitions>
				<RowDefinition Height="36" />
				<RowDefinition Height="36" />
			</Grid.RowDefinitions>
			<Label Margin="0,5" HorizontalContentAlignment="Right">Page Url</Label>
			<TextBox Name="txt_url"  Margin="5" Grid.Column="1" Grid.ColumnSpan="3" />
			<Label Margin="0,5" Grid.Row="1" HorizontalContentAlignment="Right">Save to</Label>
			<TextBox Name="txt_dir" Margin="5" Grid.Row="1" Grid.Column="1" />
			<Label Grid.Row="1" Grid.Column="2" Margin="0,5" HorizontalContentAlignment="Right" Content="Thread"></Label>
			<TextBox x:Name="txt_thread" Grid.Row="1" Grid.Column="3" Margin="5" MaxLength="3" HorizontalContentAlignment="Center" PreviewTextInput="TextBox_PreviewTextInput">1</TextBox>
		</Grid>
		<StackPanel Orientation="Horizontal" Grid.Row="1">
			<Button Content="Start" Name="btn_start" Click="btn_start_Click" IsEnabled="False" Margin="5" Padding="5,0" />
			<Button Content="Get List" Name="btn_get_list" Click="btn_get_list_Click" Margin="5" Padding="5,0" />
			<Button Content="Delete" Name="btn_delelte" Margin="5" Padding="5,0" Click="btn_delelte_Click" />
			<Button Content="Select Downloaded" Name="btn_select_downloaded" Margin="5" Padding="5,0" Click="btn_select_downloaded_Click" />
		</StackPanel>
	</Grid>
</Window>
